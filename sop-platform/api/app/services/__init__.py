# Implemented in Phase 5
# Service modules used by routes and n8n webhooks:
#
#   docx_generator.py       — python-docx template assembly (6-phase pipeline)
#   annotation_renderer.py  — Pillow callout circle rendering
#   mermaid_renderer.py     — Delegates to sop-extractor /render-mermaid endpoint
